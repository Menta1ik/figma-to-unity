# 📖 Полное руководство разработчика: Figma Importer v2.8.2

Данное руководство содержит исчерпывающую информацию по настройке и эксплуатации системы импорта UI из Figma в Unity uGUI. 

---

## 🛠 1. Установка и зависимости

Плагин работает как автономный **UPM-пакет**. 

### Требования:
*   **Unity 2021.3 LTS** или выше.
*   **TextMeshPro**: должен быть установлен в проекте (Window -> TextMeshPro -> Import TMP Essential Resources).

### Шаги по установке через Git:
1.  Откройте **Window -> Package Manager**.
2.  Нажмите кнопку **+** (Add) и выберите **Add package from git URL...**.
3.  Вставьте строку: `https://github.com/Menta1ik/figma-to-unity.git?path=plugin#v2.8.2`
4.  Нажмите **Add**. Unity загрузит плагин и автоматически добавит зависимости (Newtonsoft JSON).

---

## ⚙️ 2. Шаг 0: Создание конфигурационных ассетов

Прежде чем открывать окно импортера, необходимо создать два файла данных в вашем проекте (`Assets/`):

### А. FigmaImporterSettings (Общие настройки)
1.  В окне **Project** нажмите правую кнопку мыши -> **Create -> Figma Importer -> Settings**.
2.  Назовите ассет, например, `MyProjectSettings`.
3.  **Настройка путей (Asset Paths):**
    *   `Base Sprites Path`: Куда сохранять скачанные картинки (например, `UI/Sprites`).
    *   `Base Prefabs Path`: Куда сохранять готовые префабы (например, `UI/Prefabs`).
4.  **Политика Non-Destructive:**
    *   `Preserve Unity Names`: Если включено, плагин не будет менять имена объектов, которые вы переименовали вручную в Unity.
    *   `Preserve Manual Components`: Если включено, плагин не будет удалять компоненты (скрипты, анимации), добавленные вами поверх импортированных объектов.

### Б. Логирование (Logging)
*   `Log Level`: Управляет объёмом вывода в Console Unity.
    *   `Silent` — никаких логов. Полезно при автоматизации/CI.
    *   `Minimal` (по умолчанию) — только вехи: начало/конец синхронизации, итоги загрузки изображений, сохранение префаба, ошибки.
    *   `Verbose` — детальный вывод по каждой ноде: какой хендлер сработал, какой шрифт заменён, какой тип маски назначен. Для отладки.

### В. Экспорт изображений (Image Export)
*   `Image Export Scale`: Коэффициент масштабирования при загрузке изображений из Figma API. Диапазон: 0.5–4x, по умолчанию **2x**.
    *   `1` = оригинальный размер из Figma.
    *   `2` = ретина-качество (рекомендуется для мобильных проектов).
    *   `3–4` = высокое разрешение для десктопа/планшетов.

### Г. Адаптивность (Adaptive Layout)
Настройки адаптивности позволяют плагину автоматически настраивать `RectTransform` и корневой `Canvas`:
*   `Enable Constraints Translation`: При включении плагин переводит "Constraints" из Figma в анкоры (Anchors) и отступы (Offsets) Unity. Если выключено — используется абсолютное позиционирование.
*   `Canvas Scale Mode`: Определяет тип компонента `CanvasScaler` на корневом объекте:
    *   `Constant Pixel Size`: UI не меняет размер при изменении разрешения.
    *   `Scale With ScreenSize`: Самый частый режим. UI растягивается пропорционально.
*   `Reference Resolution`: Дизайнерское разрешение (например, 1920x1080).
*   `Match Width or Height`: Баланс масштабирования между шириной и высотой (0 — по ширине, 1 — по высоте).

### Д. FontMappingTable (Таблица шрифтов)
1.  Нажмите правую кнопку мыши -> **Create -> Figma Importer -> Font Mapping Table**.
2.  Этот ассет связывает имена шрифтов из Figma с вашими ассетами **TextMeshPro SDF**.
3.  Вы можете заполнить её вручную или использовать кнопку **Font Audit** в окне импортера для автоматического поиска используемых шрифтов.

---

## 🖥 3. Работа с окном Figma Importer

Откройте окно через меню: **Window -> Figma Importer -> Dashboard**.

### Блок 1: Connection & Config (Связь)
*   **Use Local JSON (Offline Mode)**: *Новое в v2.8.0*. При включении плагин игнорирует API и загружает данные из файла `Assets/lobby_figma.json`.
*   **Figma URL / File ID**: Вставьте полную ссылку на макет Figma или его ID.
*   **Single Node ID**: (Опционально) Если вам нужно обновить только один экран — выделите его в Figma, нажмите правой кнопкой -> **Copy link** и вставьте сюда. Плагин сам вырежет ID ноды.
*   **Access Token (PAT)**: Ваш токен Figma (*Settings -> Personal access tokens*). Токен хранится в `SessionState` — он переживает перезагрузку домена, но никогда не сохраняется на диск.
*   **Importer Settings**: Перетащите сюда созданный ассет настроек из Шага 2А.

### Блок 2: Resources & Target (Ресурсы)
*   **Font Mapping**: Перетащите сюда таблицу шрифтов из Шага 2Д.
*   **Кнопка [Font Audit]**: Нажмите её, чтобы просканировать Figma-файл. В консоли появится список всех шрифтов.
*   **Root Canvas**: Самое важное поле. Перетащите сюда объект **Canvas** из вашей сцены. Внутри него плагин будет строить UI.

### Блок 3: Sync & Generate (Запуск)
*   **Sync Images**: Включает/выключает скачивание картинок.
*   **Force Update**: Если включено, плагин полностью пересоберет сцену с нуля, игнорируя существующие объекты и кэши.
*   **Кнопка [🚀 RUN FULL SYNC]**: Запускает процесс.

---

## 🏗 4. Внутренняя архитектура (v2.8.2)

В версии **v2.8.2** логика полностью централизована («Single Brain»), а сетевые запросы ускорены технологией **Turbo Batching**.

| Класс | Ответственность |
| :--- | :--- |
| `FigmaParser` | **Центральный Оркестратор**. Управляет кэшем, API, TreeWalker и модулями синхронизации. |
| `ImageSyncService` | **Turbo Batching Engine**. Параллельная загрузка (5 потоков по 100 объектов). Потокобезопасная работа с Main Thread. |
| `FigmaTreeWalker` | Рекурсивный обход дерева нод и создание/обновление GameObjects. |
| `FigmaMaskResolver` | Жизненный цикл масок: DismantleAll → ApplyDeferred → CleanupOrphaned. |
| `FigmaOrphanManager` | Детекция элементов, удалённых в Figma, маркировка как orphan. |
| `FigmaAPIClient` | Сетевая логика с расширенной диагностикой (401, 404, 414). |

### Stencil Guard & Hierarchy Flattening
*   **FigmaMaskResolver.DismantleAll**: Находит технические контейнеры `[Mask]` и «разбирает» их перед каждым импортом.
*   **Stencil Depth Tracking**: При глубине вложенности > 3 маска переключается на `RectMask2D`, предотвращая ошибку `stencil mask depth > 8`.

---

## 🏗 5. Процесс синхронизации и результат

После нажатия кнопки происходит следующее:
1.  **Парсинг JSON**: Плагин запрашивает структуру макета (или берет из кэша/локального файла).
2.  **Сборка иерархии**: На сцене создаются объекты. Каждому назначается компонент `FigmaElement`.
3.  **Turbo Batch Image Sync**: Плагин параллельно запрашивает ссылки на изображения (батчи по 100 штук), что позволяет обрабатывать макеты с **23,000+ нодами** за считанные секунды.
4.  **Auto-Prefab**: Как только сборка завершена, плагин сохраняет результат в префаб по пути из настроек.

---

## 🏷 6. Подготовка макета в Figma (Маркеры)

Чтобы автоматизировать создание компонентов, добавляйте суффиксы к названиям слоев:

| Маркер | Результат в Unity | Описание |
| :--- | :--- | :--- |
| `[Btn]` | Добавит компонент **Button** и настроит `Raycast Target`. |
| `[Input]` | Добавит компонент **TMP_InputField**. |
| `[Scroll]` | Создаст структуру **ScrollRect**. |
| `[Toggle]` | Добавит компонент **Toggle**. |
| `[Img]` | Принудительно импортирует ноду как спрайт (даже если это вектор). |

**Графические маркеры:**
*   `_9slice` (в конце имени): Настроит **Border** в Texture Importer и установит тип `Sliced`.

---

## 🔍 7. Диагностика и проверка состояния (Health Check)

Инструмент автоматического аудита пайплайна (**Figma Importer -> Diagnostics -> Health Check**).

### Что проверяет аудит:
*   **Architecture Integrity**: Доступность сервисов `ImageSyncService` и `PrefabManager`.
*   **Asmdef Verification**: Корректность файлов Assembly Definition.
*   **Font Validation**: Наличие глобального шрифта-заглушки.
*   **Soft-Delete Enforcement**: Проверка системы защиты от удаления (Orphan tracking).

---

## 🧪 8. Тестирование и контроль качества

Используется система из **58 тестов** на базе **Unity Test Framework**.

| Файл | Тестов | Покрытие |
| :--- | :---: | :--- |
| `HandlerTests.cs` | 11 | Основные хендлеры и OrphanManager. |
| `AdaptiveLayoutTests.cs` | 4 | TransformHandler и TreeWalker context. |
| `DecompositionTests.cs` | 20 | TreeWalker, MaskResolver, OrphanManager. |
| `CacheTests.cs` | 7 | FigmaResponseCache и инвалидация. |

---

## 🆘 9. Решение проблем (Troubleshooting)

### Ошибка [Figma API Error] 414 URI Too Long:
*   **Решение**: Обновитесь до версии **2.8.2**. Проблема решена через Turbo Batching Engine.

### Сцена пустая после импорта:
1.  **Проверьте консоль**: Теперь там точно есть причина (например, `[Figma API Error] 401` или `[FigmaParser] No valid nodes found`).
2.  **Clear Cache**: Нажмите кнопку **Clear Cache**, если данные из Figma не обновляются.

### Блокировка префаба:
*   Используйте **Unpack Prefab** перед синхронизацией, если Unity запрещает менять структуру объекта.

---
**BrainySoftware OU © 2026**
**Версия документа:** 2.8.2 (Turbo Batching Release)
